import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.ke.landmarklandscaping.admin',
  appName: 'Landmark Admin',
  webDir: 'www'
};

export default config;
